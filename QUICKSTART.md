# 🚀 Quick Start Guide - DocMint

## 📋 Prerequisites

Before you begin, make sure you have:
- ✅ Node.js 18+ installed
- ✅ npm or yarn package manager
- ✅ MongoDB Atlas account (free tier works)
- ✅ Stripe account (for payments)
- ✅ Git installed

---

## ⚡ Fast Setup (5 minutes)

### Option 1: Automated Setup (Recommended)

```bash
# Run the setup script
./setup.sh
```

The script will guide you through:
1. Creating `.env.local` from template
2. Generating secure keys
3. Configuring MongoDB
4. Setting up Stripe (optional)
5. Installing dependencies
6. Seeding the database

### Option 2: Manual Setup

```bash
# 1. Install dependencies
npm install

# 2. Copy environment file
cp .env.example .env.local

# 3. Edit .env.local with your values
nano .env.local  # or use any editor

# 4. Generate NextAuth secret
openssl rand -base64 32

# 5. Seed database
npm run seed

# 6. Start development server
npm run dev
```

---

## 🔑 Required Configuration

### 1. MongoDB (Required)

1. Go to [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Create a free cluster
3. Create a database user
4. Get connection string
5. Add to `.env.local`:
   ```
   MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/docmint
   ```

### 2. NextAuth (Auto-generated by setup script)

```env
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-generated-secret
```

### 3. Stripe (Optional - for payments)

1. Sign up at [Stripe](https://stripe.com)
2. Go to Developers → API Keys
3. Copy test mode keys:
   ```env
   STRIPE_SECRET_KEY=sk_test_...
   STRIPE_PUBLISHABLE_KEY=pk_test_...
   ```
4. Create a product and price:
   - Go to Products → Create Product
   - Name: "Pro Plan"
   - Price: $9.99/month (or your choice)
   - Copy Price ID: `price_...`
5. Create webhook:
   - Go to Developers → Webhooks
   - Add endpoint: `http://localhost:3000/api/stripe/webhook`
   - Select events: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`
   - Copy webhook secret: `whsec_...`

---

## 🎯 Running the Application

```bash
# Development mode
npm run dev

# Build for production
npm run build

# Start production server
npm start

# Seed database (run once)
npm run seed

# Test database connection
npm run test:db
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

---

## 🧪 Testing

### Test Accounts

**Create a test user:**
1. Go to `/signup`
2. Enter email and password
3. Sign up

**Test Stripe payments:**
- Card number: `4242 4242 4242 4242`
- Expiry: Any future date
- CVC: Any 3 digits
- ZIP: Any 5 digits

### Test Features

1. **Dark Mode**: Click sun/moon icon in header
2. **Generate Document**:
   - Go to `/generate`
   - Select Country → Document Type → Industry
   - Fill form → Generate → Download PDF
3. **Upgrade to Pro**:
   - Go to `/billing`
   - Click "Upgrade to Pro"
   - Complete Stripe checkout

---

## 📁 Project Structure

```
crat/
├── app/                      # Next.js app directory
│   ├── (auth)/              # Auth pages (login, signup)
│   ├── (protected)/         # Protected pages (dashboard, generate, etc)
│   ├── api/                 # API routes
│   └── page.tsx             # Landing page
├── components/              # React components
│   ├── auth/               # Auth components
│   ├── dashboard/          # Dashboard components
│   ├── generator/          # Document generator components
│   ├── landing/            # Landing page components
│   ├── layout/             # Header, footer
│   ├── providers/          # Context providers (ThemeProvider)
│   └── ui/                 # Reusable UI components
├── lib/                     # Utilities and configs
│   ├── auth/               # NextAuth configuration
│   ├── db/                 # Database models and queries
│   │   └── seed/           # Database seed data
│   ├── pdf/                # PDF generation
│   ├── stripe/             # Stripe client
│   └── validation/         # Zod schemas
└── types/                   # TypeScript types
```

---

## 🎨 Features Overview

### ✅ Implemented Features

- **🌙 Dark Mode**: Full theme system with persistence
- **📄 6 Document Types**: Contract, NDA, Invoice, Proposal, Quotation, Agreement
- **🌍 6 Countries**: US, India, UK, Canada, Australia, Germany
- **💼 6 Industries**: Tech, Creative, Consulting, Legal, Finance, Healthcare
- **💳 Stripe Payments**: Subscription management with webhooks
- **🔐 Authentication**: Email/password + Google OAuth
- **📊 Dashboard**: User stats and document management
- **📥 PDF Generation**: Professional document exports
- **📱 Responsive Design**: Mobile-friendly UI
- **✨ Modern UI**: Gradients, animations, glassmorphism

---

## 🔧 Environment Variables Reference

```env
# Database
MONGODB_URI=                    # MongoDB connection string

# Auth
NEXTAUTH_URL=                   # Your app URL
NEXTAUTH_SECRET=                # Random secret (32+ chars)
GOOGLE_CLIENT_ID=               # Google OAuth (optional)
GOOGLE_CLIENT_SECRET=           # Google OAuth (optional)

# Stripe
STRIPE_SECRET_KEY=              # sk_test_...
STRIPE_PUBLISHABLE_KEY=         # pk_test_...
STRIPE_PRICE_ID_MONTHLY=        # price_...
STRIPE_WEBHOOK_SECRET=          # whsec_...

# App
NEXT_PUBLIC_APP_URL=            # Your app URL (public)
```

---

## 🐛 Common Issues

### MongoDB Connection Failed
```bash
# Check your connection string format
# Make sure to:
# 1. Replace <password> with actual password
# 2. Add database name at the end: /docmint
# 3. Whitelist your IP in MongoDB Atlas
```

### Stripe Webhook Not Working
```bash
# For local development, use Stripe CLI:
stripe login
stripe listen --forward-to localhost:3000/api/stripe/webhook

# Copy the webhook secret it provides to .env.local
```

### Dark Mode Not Persisting
```bash
# Clear browser cache and localStorage:
# Open DevTools → Application → Storage → Clear Site Data
```

### Templates Not Loading
```bash
# Re-run seed script:
npm run seed

# Check database connection:
npm run test:db
```

---

## 📚 Documentation

- **COMPLETE_IMPLEMENTATION_GUIDE.md** - Full setup and features
- **FRONTEND_SAAS_TRANSFORMATION_COMPLETE.md** - UI/UX changes
- **VISUAL_CHANGES_GUIDE.md** - Before/after comparisons
- **.env.example** - Environment variables template

---

## 🚀 Deployment

### Vercel (Recommended)

```bash
# 1. Push to GitHub
git add .
git commit -m "Initial commit"
git push origin main

# 2. Go to https://vercel.com
# 3. Import your repository
# 4. Add environment variables from .env.local
# 5. Deploy!

# 6. Update Stripe webhook:
#    - Change endpoint to: https://yourdomain.com/api/stripe/webhook
#    - Get new webhook secret
#    - Update STRIPE_WEBHOOK_SECRET in Vercel
```

### Other Platforms

- **Environment Variables**: Copy all vars from `.env.local`
- **Build Command**: `npm run build`
- **Start Command**: `npm start`
- **Node Version**: 18+

---

## 💡 Tips

1. **Use Test Mode**: Keep Stripe in test mode during development
2. **Webhook Testing**: Use Stripe CLI for local webhook testing
3. **Database Backups**: MongoDB Atlas auto-backups on free tier
4. **API Keys**: Never commit `.env.local` to git (it's in `.gitignore`)
5. **Production**: Switch to live Stripe keys before going live

---

## 📞 Support

- **Issues**: Check troubleshooting section above
- **Documentation**: Read the implementation guides
- **Stripe Docs**: https://stripe.com/docs
- **MongoDB Docs**: https://docs.mongodb.com

---

## 🎉 You're All Set!

Your DocMint application is ready to use. Start the development server and begin generating professional legal documents!

```bash
npm run dev
```

Visit: http://localhost:3000

**Happy coding! 🚀**
